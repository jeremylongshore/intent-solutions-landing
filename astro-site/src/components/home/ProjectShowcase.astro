---
const projects = [
  {
    name: 'Claude Code Plugins Hub',
    description:
      '430+ plugins with 2,750+ agent skills, CCPI package manager, and interactive tutorials—2,200+ GitHub stars, 300+ forks.',
    badge: 'Python · Open Source · 2,200★',
    link: 'https://github.com/jeremylongshore/claude-code-plugins-plus-skills',
    external: true,
    gradient: 'linear-gradient(135deg, rgba(34,211,238,0.15) 0%, rgba(6,182,212,0.08) 60%, transparent 100%)',
    accentColor: 'rgba(34,211,238,0.7)',
    iconPath: 'M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5',
  },
  {
    name: 'IRSB',
    description:
      'On-chain guardrails for AI agents—EIP-7702 spend limits, cryptographic execution receipts, and automated dispute resolution.',
    badge: 'TypeScript · Ethereum · EIP-7702',
    link: 'https://github.com/jeremylongshore/irsb',
    external: true,
    gradient: 'linear-gradient(135deg, rgba(139,92,246,0.15) 0%, rgba(109,40,217,0.08) 60%, transparent 100%)',
    accentColor: 'rgba(139,92,246,0.7)',
    iconPath: 'M12 2L5 12l7 4 7-4-7-10zM5 12l7 8 7-8',
  },
  {
    name: 'Moat',
    description:
      'Verified Agent Capabilities Marketplace—MCP-first trust, policy, and execution layer for agents.',
    badge: 'Python · MCP · Agent Trust',
    link: 'https://github.com/jeremylongshore/moat',
    external: true,
    gradient: 'linear-gradient(135deg, rgba(245,158,11,0.15) 0%, rgba(217,119,6,0.08) 60%, transparent 100%)',
    accentColor: 'rgba(245,158,11,0.7)',
    iconPath: 'M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z',
  },
  {
    name: "Bob's Brain",
    description:
      'Collaborative AI assistant—Google Gemini 2.5 Flash, Neo4j knowledge graphs, BigQuery analytics, Cloud Run deployment.',
    badge: 'Python · Gemini · Neo4j',
    link: 'https://github.com/jeremylongshore/bobs-brain',
    external: true,
    gradient: 'linear-gradient(135deg, rgba(16,185,129,0.15) 0%, rgba(5,150,105,0.08) 60%, transparent 100%)',
    accentColor: 'rgba(16,185,129,0.7)',
    iconPath: 'M9.5 2A2.5 2.5 0 0 1 12 4.5v15a2.5 2.5 0 0 1-4.96-.44l-3-9.5A2.5 2.5 0 0 1 6.5 6H17a2.5 2.5 0 0 1 2.46 2.06l-3 9.5A2.5 2.5 0 0 1 12 19.5',
  },
  {
    name: 'News Pipeline',
    description:
      'Automated monitoring that converts daily news into structured intelligence briefs for decision makers.',
    badge: 'Workflow Automation · 10★',
    link: 'https://github.com/jeremylongshore/news-pipeline-n8n',
    external: true,
    gradient: 'linear-gradient(135deg, rgba(59,130,246,0.15) 0%, rgba(37,99,235,0.08) 60%, transparent 100%)',
    accentColor: 'rgba(59,130,246,0.7)',
    iconPath: 'M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 0-2 2zm0 0a2 2 0 0 1-2-2v-9c0-1.1.9-2 2-2h2M18 14h-8M15 18h-5M10 6h8v4h-8z',
  },
  {
    name: 'DiagnosticPro',
    description:
      'Live automotive diagnostics for service centers—Vertex AI orchestrates triage while Firebase keeps technicians synced.',
    badge: 'TypeScript · React · Vertex AI',
    link: 'https://diagnosticpro.io',
    external: true,
    gradient: 'linear-gradient(135deg, rgba(239,68,68,0.15) 0%, rgba(185,28,28,0.08) 60%, transparent 100%)',
    accentColor: 'rgba(239,68,68,0.7)',
    iconPath: 'M12 22C6.477 22 2 17.523 2 12S6.477 2 12 2s10 4.477 10 10-4.477 10-10 10zm0-6a4 4 0 1 0 0-8 4 4 0 0 0 0 8zm0-2a2 2 0 1 1 0-4 2 2 0 0 1 0 4z',
  },
];
---

<section id="projects" class="showcase-section" aria-labelledby="showcase-title">
  <div class="showcase-container">

    <!-- Section header -->
    <div class="showcase-header">
      <div class="showcase-eyebrow" aria-hidden="true">
        <span class="eyebrow-rule"></span>
        <span class="eyebrow-label">Open Source Work</span>
        <span class="eyebrow-rule"></span>
      </div>
      <h2 id="showcase-title" class="showcase-title">products i've shipped</h2>
      <p class="showcase-subtitle">Real systems built and deployed—not demos, not proofs of concept.</p>
    </div>

    <ul class="project-grid" role="list">
      {projects.map((project, index) => (
        <li class="project-card" data-index={index} role="listitem">
          <a
            href={project.link}
            target={project.external ? '_blank' : undefined}
            rel={project.external ? 'noopener noreferrer' : undefined}
            class="card-link"
            aria-label={`${project.name} — ${project.badge}`}
          >
            <!-- Gradient header panel -->
            <div class="card-visual" style={`background: ${project.gradient};`}>
              <!-- Grid pattern overlay -->
              <div class="card-grid-overlay" aria-hidden="true"></div>
              <!-- Project icon -->
              <div class="card-icon-wrap" style={`border-color: ${project.accentColor.replace('0.7)', '0.25)')};`}>
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" aria-hidden="true"
                  style={`color: ${project.accentColor};`}>
                  <path d={project.iconPath} stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </div>
              <!-- Corner accent -->
              <div class="card-corner-tl" aria-hidden="true"></div>
              <div class="card-corner-br" aria-hidden="true"></div>
            </div>

            <!-- Card body -->
            <div class="card-body">
              <h3 class="project-name">{project.name}</h3>
              <p class="project-desc">{project.description}</p>
              <div class="card-footer">
                <span class="tech-badge">{project.badge}</span>
                <span class="view-link" aria-hidden="true">
                  view
                  <svg width="12" height="12" viewBox="0 0 14 14" fill="none" aria-hidden="true">
                    <path d="M2.5 7h9M7.5 3l4 4-4 4" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </span>
              </div>
            </div>

            <!-- Hover border glow -->
            <div class="card-hover-border" aria-hidden="true"></div>
          </a>
        </li>
      ))}
    </ul>

  </div>
</section>

<style>
  .showcase-section {
    padding: 7rem 0;
    background: rgb(9 9 11); /* zinc-950 */
    border-top: 1px solid rgba(39, 39, 42, 0.5);
  }

  .showcase-container {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* ── Header ── */
  .showcase-header {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  .showcase-eyebrow {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    margin-bottom: 1.2rem;
  }

  .eyebrow-rule {
    display: block;
    height: 1px;
    width: 3.5rem;
    background: linear-gradient(to right, transparent, rgba(34,211,238,0.2));
  }

  .eyebrow-rule:last-child {
    background: linear-gradient(to left, transparent, rgba(34,211,238,0.2));
  }

  .eyebrow-label {
    font-family: 'Syne', system-ui, sans-serif;
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.28em;
    text-transform: uppercase;
    color: rgb(34 211 238);
  }

  .showcase-title {
    font-family: 'Syne', system-ui, sans-serif;
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 700;
    color: rgb(250 250 250);
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin-bottom: 0.75rem;
  }

  .showcase-subtitle {
    font-size: 0.925rem;
    color: rgb(113 113 122);
  }

  /* ── Grid ── */
  .project-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  /* ── Card scroll-reveal ── */
  .project-card {
    opacity: 0;
    transform: translateY(24px);
    transition:
      opacity 0.55s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.55s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .project-card.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  /* ── Card link wrapper ── */
  .card-link {
    position: relative;
    display: flex;
    flex-direction: column;
    height: 100%;
    text-decoration: none;
    border-radius: 0.875rem;
    border: 1px solid rgba(39, 39, 42, 0.75);
    background: rgba(18, 18, 20, 0.85);
    backdrop-filter: blur(8px);
    overflow: hidden;
    transition:
      border-color 0.28s ease,
      box-shadow 0.28s ease,
      transform 0.22s ease;
  }

  .card-link:hover {
    border-color: rgba(34, 211, 238, 0.2);
    box-shadow: 0 12px 40px rgba(0,0,0,0.5), 0 0 0 1px rgba(34,211,238,0.04);
    transform: translateY(-3px);
  }

  .card-link:focus-visible {
    outline: 2px solid rgb(34 211 238);
    outline-offset: 3px;
  }

  /* ── Visual header panel ── */
  .card-visual {
    position: relative;
    height: 6rem;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    flex-shrink: 0;
  }

  /* Subtle grid texture */
  .card-grid-overlay {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.025) 1px, transparent 1px);
    background-size: 20px 20px;
    opacity: 0.6;
  }

  /* Icon centered in visual */
  .card-icon-wrap {
    position: relative;
    z-index: 2;
    width: 2.75rem;
    height: 2.75rem;
    border-radius: 0.625rem;
    border: 1px solid rgba(255,255,255,0.1);
    background: rgba(9, 9, 11, 0.45);
    backdrop-filter: blur(6px);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.22s ease, background 0.22s ease;
  }

  .card-link:hover .card-icon-wrap {
    transform: scale(1.08);
    background: rgba(9, 9, 11, 0.65);
  }

  /* Corner brackets (targeting-reticle style) */
  .card-corner-tl,
  .card-corner-br {
    position: absolute;
    width: 10px;
    height: 10px;
    z-index: 2;
  }

  .card-corner-tl {
    top: 8px;
    left: 8px;
    border-top: 1px solid rgba(255,255,255,0.12);
    border-left: 1px solid rgba(255,255,255,0.12);
  }

  .card-corner-br {
    bottom: 8px;
    right: 8px;
    border-bottom: 1px solid rgba(255,255,255,0.12);
    border-right: 1px solid rgba(255,255,255,0.12);
  }

  /* ── Card body ── */
  .card-body {
    padding: 1.25rem 1.5rem 1.5rem;
    display: flex;
    flex-direction: column;
    flex: 1;
    border-top: 1px solid rgba(39, 39, 42, 0.5);
  }

  .project-name {
    font-family: 'Syne', system-ui, sans-serif;
    font-size: 1rem;
    font-weight: 600;
    color: rgb(228 228 231);
    margin: 0 0 0.6rem;
    line-height: 1.35;
    transition: color 0.2s ease;
  }

  .card-link:hover .project-name {
    color: rgb(250 250 250);
  }

  .project-desc {
    font-size: 0.8125rem;
    line-height: 1.65;
    color: rgb(113 113 122);
    margin: 0 0 1.25rem;
    flex: 1;
    transition: color 0.2s ease;
  }

  .card-link:hover .project-desc {
    color: rgb(161 161 170);
  }

  /* ── Card footer ── */
  .card-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-top: auto;
  }

  .tech-badge {
    font-family: 'Syne', system-ui, sans-serif;
    font-size: 0.65rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: rgb(82 82 91);
    background: rgba(39, 39, 42, 0.5);
    border: 1px solid rgba(63, 63, 70, 0.6);
    border-radius: 3px;
    padding: 0.22rem 0.5rem;
    white-space: nowrap;
    transition: border-color 0.25s ease, color 0.25s ease;
  }

  .card-link:hover .tech-badge {
    border-color: rgba(34, 211, 238, 0.2);
    color: rgb(113 113 122);
  }

  .view-link {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    font-size: 0.75rem;
    font-weight: 500;
    color: rgb(63 63 70);
    white-space: nowrap;
    flex-shrink: 0;
    transition: color 0.2s ease;
  }

  .card-link:hover .view-link {
    color: rgb(34 211 238);
  }

  /* ── Hover border glow (pseudo-element trick alternative) ── */
  .card-hover-border {
    display: none; /* Reserved for future gradient border effect */
  }

  /* ── Responsive ── */
  @media (max-width: 1024px) {
    .project-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .showcase-section {
      padding: 5rem 0;
    }

    .showcase-container {
      padding: 0 1.25rem;
    }

    .project-grid {
      grid-template-columns: 1fr;
      gap: 0.875rem;
    }

    .card-link:hover {
      transform: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .project-card {
      opacity: 1;
      transform: none;
    }
  }
</style>

<script>
  function setupShowcaseObserver() {
    const cards = document.querySelectorAll<HTMLElement>('.project-card:not([data-showcase-bound])');
    if (!cards.length) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const card = entry.target as HTMLElement;
            const index = parseInt(card.dataset.index ?? '0', 10);
            setTimeout(() => card.classList.add('revealed'), index * 80);
            observer.unobserve(card);
          }
        });
      },
      { threshold: 0.08, rootMargin: '0px 0px -40px 0px' }
    );

    cards.forEach((card) => {
      card.dataset.showcaseBound = 'true';
      observer.observe(card);
    });
  }

  document.addEventListener('DOMContentLoaded', setupShowcaseObserver);
  document.addEventListener('astro:page-load', setupShowcaseObserver);
</script>

<noscript>
  <style>
    /* Progressive enhancement: cards are visible without JS or if observer fails. */
    .project-card { opacity: 1 !important; transform: none !important; }
  </style>
</noscript>
