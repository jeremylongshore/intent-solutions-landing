# Environment variables — astro-site
# Copy to .env for local development.

# This site does NOT use Resend, SendGrid, or any third-party transactional
# email provider. Form submissions land in Slack via the forms-api on the
# VPS (see CLAUDE.md § Form Submission Flow). There is no email auto-reply
# from the marketing site, by design.

# No required env vars at present.
